<!DOCTYPE html>
<html>
  <head>
    <title>Nursey</title>
    <meta name="description" content="Nursey est une application de suivi de l'alimentation de votre bébé. En toute simplicité.">
    <meta name="viewport" content="width=device-width, initial-scale=1, shrink-to-fit=no">
    <%= csrf_meta_tags %>
    <%= csp_meta_tag %>

    <link rel="stylesheet" href="https://use.fontawesome.com/releases/v6.7.0/css/all.css">
    <%= stylesheet_link_tag "application", "data-turbo-track": "reload" %>
    <%= javascript_importmap_tags %>
  </head>

  <body>
  <%= render "shared/flashes" %>
  <%= turbo_frame_tag "redirect_chat_window" %>
  <%= yield %>
  </body>
  <script src="https://cdn.jsdelivr.net/npm/sweetalert2@11"></script>
</html>
