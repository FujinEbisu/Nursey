<%= simple_form_for [@chat, @message],
  html: {class: "d-flex"} do |f|
%>
  <% other_user = current_user.userable == @chat.doctor ? @chat.mother : @chat.doctor %>
  <%= f.input :content,
    label: false,
    placeholder: "Votre message à #{other_user.last_name.capitalize}",
    wrapper_html: {class: "flex-grow-1 h-300"}, input_html: { class: "form-control custom-style" }
  %>
  <%= f.submit "Envoyer", class: "button-standard btnchat" %>
<% end %>
