<!-- Button trigger modal -->
<button type="button" class="button-standard" data-bs-toggle="modal" data-bs-target="#ModalMother">
 <a href=""> Launch modal</a>
</button>

<!-- Modal -->
<div class="modal fade" id="ModalMother" tabindex="-1" aria-labelledby="exampleModalLabel" aria-hidden="true">
  <div class="modal-dialog modal-dialog-centered">
    <div class="modal-content">
      <%# <div class="modal-header">
        <h5 class="modal-title" id="exampleModalLabel">Modal title</h5>
      </div> %>
      <div class="modal-body">
        ...INSERT TURBO HERE...
        <!-- simple form with label on the left and input on the right -->
        <div class="row">
        <h1>Total:</h1> <!--timer-->
        </div>
        <%= simple_form_for [@mother, @feed] do |f| %>
          <%= f.input :quantity_left, wrapper_html: { class: 'form-group' }, input_html: { class: 'form-input-modal' }%>
          <%= f.input :quantity_right, wrapper_html: { class: 'form-group' }, input_html: { class: 'form-input-modal' } %>
          <%= f.input :comment %>
          <%# f.input :mood, as: :checkbox %>
        <%# <div class="form-inline">
          <%= f.input :mood, as: :radio_buttons, collection: Feed.moods.keys %>
        <%# </div> %>
        <%# crvgbhnj %>

      <div class="emoji-radio-group">
  <% Feed.moods.keys.each do |mood| %>
      <%= f.radio_button :mood, mood, id: "feed_mood_#{mood}", class: "emoji-radio" %>
    <label class="emoji-option" for="feed_mood_<%= mood %>" %>
      <span class="emoji"><%= emoji_for_mood(mood) %></span>
    </label>
  <% end %>
</div>

<%= f.error :mood %>


            <%# ygnj,k %>
          <%= f.input :child_id, as: :select, collection: @child_id %>
          <%= f.button :submit, input_html: { class: 'button-standard'} %>
        <% end %>
      <div class="modal-footer border-top-0">
        <button type="button" class="button-standard" data-bs-dismiss="modal"><a href="">Close</a></button>
        <button type="button" class="button-standard-dark"><a href="">Save changes</a></button>
      </div>
    </div>
  </div>
</div>
